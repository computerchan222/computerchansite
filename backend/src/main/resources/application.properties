server.port=${PORT:8080}

# Main Application Database - Supabase (with connection limits)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-eu-west-2.pooler.supabase.com:6543/postgres?user=postgres.mcslngjlkeyjoqarwjmi&password=T0HmWMftefBMfIAZ}
spring.datasource.username=${DATABASE_USER:postgres.mcslngjlkeyjoqarwjmi}
spring.datasource.password=${DATABASE_PASSWORD:T0HmWMftefBMfIAZ}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Settings - CRITICAL for Supabase limits!
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Subsquid Database - Supabase Production
subsquid.db.url=${SUBSQUID_DATABASE_URL:jdbc:postgresql://aws-1-eu-west-2.pooler.supabase.com:6543/postgres?user=postgres.qcvuctvvzphaagwyggyo&password=Subsquid#!}
subsquid.db.username=${SUBSQUID_DATABASE_USER:postgres.qcvuctvvzphaagwyggyo}
subsquid.db.password=${SUBSQUID_DATABASE_PASSWORD:Subsquid#!}

# CORS Configuration (for frontend)
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173,file://
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true